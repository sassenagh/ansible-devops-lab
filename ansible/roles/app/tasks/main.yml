- name: Create app directory
  ansible.builtin.file:
    path: /ansible/app
    state: directory
    mode: "0755"

- name: Render HTML from template
  ansible.builtin.template:
    src: index.html.j2
    dest: /ansible/app/index.html
    mode: "0644"
